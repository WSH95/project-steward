Mid-session Project Steward checkpoint (session continues):

1. Briefly refresh .project-steward/HANDOFF.md (Now / In flight / Next
   steps) or run `project-steward checkpoint --note "what just happened"`.
2. Keep session_status: active; prefix the PROGRESS.md entry with
   [auto-checkpoint] if this was hook- or reminder-triggered.
3. Write plain, factual notes. If a `humanizer` skill is available, apply it
   only to the new prose and preserve facts and file structure.
4. Tell the user in one line, then continue the actual work.
